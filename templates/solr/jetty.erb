# Defaults for jetty see /etc/init.d/jetty for more

# change to 0 to allow Jetty to start
NO_START=0

# change to 'no' or uncomment to use the default setting in /etc/default/rcS 
VERBOSE=yes

# Run Jetty as this user ID (default: jetty)
# Set this to an empty string to prevent Jetty from starting automatically
JETTY_USER=<%= scope.lookupvar('iqss::solr::jetty_user') %>

# Listen to connections from this network host
# Use 0.0.0.0 as host to accept all connections.
# Uncomment to restrict access to localhost
JETTY_HOST=<%= scope.lookupvar('iqss::solr::jetty_host') %>

# The network port used by Jetty
JETTY_PORT=<%= scope.lookupvar('iqss::solr::jetty_port') %>

# Additional arguments to pass to Jetty    
#JETTY_ARGS=

# log file
JETTY_LOGS="<%= scope.lookupvar('iqss::solr::jetty_home') %>/logs"

JETTY_HOME="<%= scope.lookupvar('iqss::solr::jetty_home') %>"

JETTY_BASE="<%= scope.lookupvar('iqss::solr::jetty_home') %>"

# Extra options to pass to the JVM         
JAVA_OPTIONS="-Dsolr.solr.home=<%= scope.lookupvar('iqss::solr::solr_home') %> <%= scope.lookupvar('iqss::solr::jetty_java_options') %>"
